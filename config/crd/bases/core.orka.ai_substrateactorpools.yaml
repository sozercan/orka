---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: substrateactorpools.core.orka.ai
spec:
  group: core.orka.ai
  names:
    kind: SubstrateActorPool
    listKind: SubstrateActorPoolList
    plural: substrateactorpools
    singular: substrateactorpool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.actorCount
      name: Actors
      type: integer
    - jsonPath: .status.workerCount
      name: Workers
      type: integer
    - jsonPath: .status.actorsPerWorker
      name: Density
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SubstrateActorPool is the Schema for Substrate actor pools.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SubstrateActorPoolSpec defines an operator-owned oversubscription
              pool.
            properties:
              precreateActors:
                description: |-
                  PrecreateActors asks the controller to create deterministic warm actors up
                  to TargetActors. Substrate may suspend them when the WorkerPool is full.
                type: boolean
              targetActors:
                description: |-
                  TargetActors is the desired number of stateful actors tracked for this
                  pool. It may exceed the physical worker budget to express oversubscription.
                format: int32
                maximum: 1000
                minimum: 0
                type: integer
              templateRef:
                description: TemplateRef is the ActorTemplate used for pool members.
                properties:
                  name:
                    description: Name is the name of the workspace template.
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the workspace template and claim.
                      It defaults to the Task namespace, or the controller namespace when configured.
                    type: string
                type: object
              workerPoolRef:
                description: WorkerPoolRef is the Substrate WorkerPool this Orka pool
                  targets.
                properties:
                  name:
                    description: Name is the name of the workspace template.
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the workspace template and claim.
                      It defaults to the Task namespace, or the controller namespace when configured.
                    type: string
                type: object
            required:
            - templateRef
            type: object
          status:
            description: SubstrateActorPoolStatus reports safe pool telemetry.
            properties:
              actorCount:
                description: ActorCount is the number of actors reported by Substrate
                  for this pool.
                format: int32
                type: integer
              actorsPerWorker:
                description: ActorsPerWorker is ActorCount divided by WorkerCount,
                  formatted as a decimal string.
                type: string
              conditions:
                description: Conditions represent the current state of the pool.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              message:
                description: Message contains sanitized reconciliation context.
                type: string
              observedGeneration:
                description: ObservedGeneration is the latest generation reconciled
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is the current controller-observed phase.
                enum:
                - Pending
                - Ready
                - Failed
                type: string
              runningActorCount:
                description: RunningActorCount is the number of pool actors currently
                  running.
                format: int32
                type: integer
              suspendedActorCount:
                description: SuspendedActorCount is the number of pool actors currently
                  suspended.
                format: int32
                type: integer
              workerCount:
                description: WorkerCount is the number of workers reported by Substrate
                  for this pool.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
