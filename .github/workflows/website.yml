name: Website

on:
  push:
    branches:
      - main
    paths:
      - "_typos.toml"
      - "website/**"
      - ".github/workflows/website.yml"
  pull_request:
    paths:
      - "_typos.toml"
      - "website/**"
      - ".github/workflows/website.yml"
  workflow_dispatch:

permissions: read-all

concurrency:
  group: ${{ github.event_name == 'pull_request' && format('website-pr-{0}', github.event.pull_request.number) || 'gh-pages' }}
  cancel-in-progress: false

jobs:
  check_typos:
    name: Check typos
    runs-on: ubuntu-latest
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Check typos
        uses: crate-ci/typos@8a48f81b6c64dcfea44b3633223084c4be58ac5f # v1.49.0
        with:
          files: website .github/workflows/website.yml

  build:
    name: Build Docusaurus site
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # PR builds run PR-controlled yarn scripts; don't leave a write-scoped
          # git credential on disk for them. The publish step below authenticates
          # with its own explicit tokenized remote instead.
          persist-credentials: false

      - name: Enable Corepack
        # Pin Yarn to the version in package.json's packageManager field so the
        # build uses the repo-declared Yarn 1.x against the committed v1 lockfile,
        # not whatever Yarn the runner's Corepack defaults to (e.g. Yarn 4).
        run: corepack enable

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: yarn
          cache-dependency-path: website/yarn.lock

      - name: Install dependencies
        working-directory: website
        run: yarn install --frozen-lockfile

      - name: Build website
        working-directory: website
        run: yarn build

  publish:
    name: Publish Docusaurus site
    needs: [check_typos, build]
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@e14015d583714f6e62063499dc959a02595150a1 # v2.21.1
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Enable Corepack
        run: corepack enable

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: yarn
          cache-dependency-path: website/yarn.lock

      - name: Install dependencies
        working-directory: website
        run: yarn install --frozen-lockfile

      - name: Build website
        working-directory: website
        run: yarn build

      - name: Publish to gh-pages
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

          rm -rf gh-pages
          git init gh-pages
          cd gh-pages
          git remote add origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"

          if git fetch --depth=1 origin gh-pages:refs/remotes/origin/gh-pages; then
            git checkout -B gh-pages origin/gh-pages
          else
            git checkout --orphan gh-pages
            git rm -rf . || true
          fi

          # Replace the website at the Pages root while preserving Pages
          # metadata and Helm chart repository artifacts.
          find . -mindepth 1 -maxdepth 1 \
            ! -name '.git' \
            ! -name '.nojekyll' \
            ! -name 'CNAME' \
            ! -name 'charts' \
            ! -name 'index.yaml' \
            ! -name '*.tgz' \
            ! -name '*.prov' \
            -exec rm -rf {} +

          rsync -a ../website/build/ ./
          touch .nojekyll

          git add -A
          if git diff --cached --quiet; then
            echo "No website changes to publish."
            exit 0
          fi

          git commit -m "Deploy website"
          git push origin HEAD:gh-pages
