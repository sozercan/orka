---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: repositoryscans.core.orka.ai
spec:
  group: core.orka.ai
  names:
    kind: RepositoryScan
    listKind: RepositoryScanList
    plural: repositoryscans
    singular: repositoryscan
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.repository
      name: Repo
      type: string
    - jsonPath: .spec.branch
      name: Branch
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.findingCounts.total
      name: Findings
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RepositoryScan is the Schema for the repository security scanning
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RepositoryScanSpec defines the desired state of RepositoryScan.
            properties:
              analysisAgentRef:
                description: AnalysisAgentRef is the agent used for scan runs.
                properties:
                  name:
                    description: Name is the name of the Agent
                    type: string
                  namespace:
                    description: Namespace is the namespace of the Agent (defaults
                      to Task namespace)
                    type: string
                required:
                - name
                type: object
              branch:
                description: Branch is the base branch to scan. Defaults to the literal
                  main branch when omitted.
                type: string
              customScanInstructionsRef:
                description: CustomScanInstructionsRef references additive scanner
                  instructions in a same-namespace ConfigMap.
                properties:
                  key:
                    description: Key is the ConfigMap data key. Defaults to "policy"
                      when omitted.
                    type: string
                  name:
                    description: Name is the ConfigMap name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              falsePositivePolicyRef:
                description: FalsePositivePolicyRef references additive false-positive
                  policy in a same-namespace ConfigMap.
                properties:
                  key:
                    description: Key is the ConfigMap data key. Defaults to "policy"
                      when omitted.
                    type: string
                  name:
                    description: Name is the ConfigMap name.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              forgeCredentialRef:
                description: |-
                  ForgeCredentialRef references the GitHub API credential used only for
                  controller-owned pull request reconciliation. Patch workflows require this
                  explicit reference.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              forkRepo:
                description: ForkRepo is the writable fork repository URL used for
                  patch proposals.
                type: string
              gitSecretRef:
                description: |-
                  GitSecretRef is the backward-compatible source-read credential reference.
                  ReadCredentialRef takes precedence when both are set. GitSecretRef is never
                  used for publication writes or forge mutations.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              historyDays:
                description: HistoryDays controls how far back the initial scan should
                  inspect repository history.
                format: int32
                type: integer
              maxFindingsPerRun:
                description: MaxFindingsPerRun bounds scan output volume.
                format: int32
                type: integer
              owner:
                description: Owner is the repository owner or organization.
                type: string
              patchAgentRef:
                description: PatchAgentRef is the agent used for patch proposal runs.
                properties:
                  name:
                    description: Name is the name of the Agent
                    type: string
                  namespace:
                    description: Namespace is the namespace of the Agent (defaults
                      to Task namespace)
                    type: string
                required:
                - name
                type: object
              prBaseBranch:
                description: PRBaseBranch is the pull request base branch for remediation.
                type: string
              provider:
                default: github
                description: Provider is the source control provider. GitHub is the
                  only supported v1 provider.
                enum:
                - github
                type: string
              publicationCredentialRef:
                description: |-
                  PublicationCredentialRef references the target-repository write credential
                  used only for exact compare-and-swap publication. Patch workflows require
                  this explicit reference.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              publicationReadCredentialRef:
                description: |-
                  PublicationReadCredentialRef references the target-repository read
                  credential used only for publication preflight and independent verification.
                  Patch workflows require this explicit reference.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              readCredentialRef:
                description: |-
                  ReadCredentialRef references the source-repository clone/read credential.
                  It is resolved only by the clean-room workspace boundary. When omitted,
                  GitSecretRef remains the backward-compatible read-only fallback.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              ref:
                description: Ref is a specific git ref, tag, or commit SHA to checkout
                  for scan tasks.
                type: string
              repoURL:
                description: RepoURL is the repository URL to scan.
                type: string
              repository:
                description: Repository is the repository name.
                type: string
              schedule:
                description: Schedule is the cron expression for incremental scans.
                type: string
              subPath:
                description: SubPath scopes scanning to a subdirectory in a monorepo.
                type: string
              suspend:
                description: Suspend pauses scheduled incremental scans.
                type: boolean
              timeZone:
                description: TimeZone is the IANA time zone for the schedule.
                type: string
              validationMaxFindingsPerRun:
                description: ValidationMaxFindingsPerRun bounds automatic validation
                  tasks for light mode.
                format: int32
                minimum: 0
                type: integer
              validationMinConfidence:
                description: ValidationMinConfidence is the minimum confidence eligible
                  for automatic validation.
                enum:
                - high
                - medium
                - low
                type: string
              validationMinSeverity:
                description: ValidationMinSeverity is the minimum severity eligible
                  for automatic validation.
                enum:
                - critical
                - high
                - medium
                - low
                type: string
              validationMode:
                default: light
                description: ValidationMode controls how aggressively findings are
                  validated.
                enum:
                - "off"
                - light
                - full
                type: string
            required:
            - analysisAgentRef
            - repoURL
            type: object
            x-kubernetes-validations:
            - message: repoURL is immutable; create a new RepositoryScan for a different
                repository
              rule: self.repoURL == oldSelf.repoURL
          status:
            description: RepositoryScanStatus defines the observed state of RepositoryScan.
            properties:
              conditions:
                description: Conditions represent the current state of the repository
                  scan.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              findingCounts:
                description: FindingCounts summarizes open findings.
                properties:
                  critical:
                    format: int32
                    type: integer
                  high:
                    format: int32
                    type: integer
                  low:
                    format: int32
                    type: integer
                  medium:
                    format: int32
                    type: integer
                  total:
                    format: int32
                    type: integer
                type: object
              lastObservedHeadSHA:
                description: LastObservedHeadSHA is the latest repository head SHA
                  seen by a completed scan.
                type: string
              lastProcessedCommit:
                description: LastProcessedCommit is the latest commit fully processed
                  by a completed scan.
                type: string
              lastScanAt:
                description: LastScanAt is the completion time of the most recent
                  scan run, regardless of success or failure.
                format: date-time
                type: string
              lastScanID:
                description: LastScanID is the most recent scan run identifier stored
                  in SQLite.
                type: string
              lastScanTaskName:
                description: LastScanTaskName is the most recent scan task name.
                type: string
              lastSuccessfulScanAt:
                description: LastSuccessfulScanAt is the completion time of the most
                  recent successful scan.
                format: date-time
                type: string
              phase:
                description: Phase describes the high-level repository scan lifecycle
                  state.
                type: string
              threatModelVersion:
                description: ThreatModelVersion is the latest persisted threat model
                  version.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
